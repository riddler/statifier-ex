### Added

- `Statifier.Effect.DatamodelChange` joins the core effect vocabulary as
  `{:datamodel_change, %Statifier.Effect.DatamodelChange{}}`, and
  `[:statifier, :session, :effect, :datamodel_change]` joins the published
  `:telemetry` event names.
