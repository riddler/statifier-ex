{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/riddler/statifier-ex/conformance/schema/manifest.json",
  "title": "Statifier conformance manifest",
  "description": "conformance/manifest.json, the index a sibling implementation reads first: the corpus hash a claim is pinned to, the statifier-ex version the corpus was emitted from, one entry per corpus file, and the upstream suites the SCION and W3C cases were taken from. Generated by the emitter and never edited by hand (ADR-0070).",
  "type": "object",
  "properties": {
    "corpus_hash": {
      "type": "string",
      "description": "sha256:<hex> over the corpus files' content, in suite order. A registry pins its claims to this value.",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "statifier_version": {
      "type": "string",
      "description": "The statifier-ex version the corpus was emitted from.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(-[0-9A-Za-z.-]+)?$"
    },
    "suites": {
      "type": "array",
      "description": "One entry per corpus file, sorted by suite.",
      "items": {
        "type": "object",
        "properties": {
          "suite": { "type": "string", "enum": ["scion", "w3c", "statifier"] },
          "file": {
            "type": "string",
            "description": "The corpus file, as a path relative to conformance/.",
            "pattern": "^corpus/[a-z0-9-]+\\.json$"
          },
          "case_count": {
            "type": "integer",
            "description": "The number of cases in the file. Never zero: a suite with no cases has no file.",
            "minimum": 1
          }
        },
        "required": ["suite", "file", "case_count"],
        "additionalProperties": false
      },
      "minItems": 1
    },
    "upstreams": {
      "type": "array",
      "description": "The upstream suites SCION and W3C cases were taken from, one entry per suite, sorted by suite.",
      "items": {
        "type": "object",
        "properties": {
          "suite": { "type": "string", "enum": ["scion", "w3c"] },
          "name": { "type": "string", "minLength": 1 },
          "url": {
            "type": "string",
            "description": "Where the upstream suite is published.",
            "pattern": "^https://"
          },
          "revision": {
            "type": "string",
            "description": "The upstream commit the documents were read at, where the upstream has one.",
            "minLength": 1
          },
          "license": { "type": "string", "enum": ["Apache-2.0", "BSD-3-Clause-W3C"] },
          "notice": {
            "type": "string",
            "description": "The licence text and copyright notice, as a path relative to conformance/.",
            "pattern": "^LICENSES/[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "required": ["suite", "name", "url", "license", "notice"],
        "additionalProperties": false
      }
    }
  },
  "required": ["corpus_hash", "statifier_version", "suites", "upstreams"],
  "additionalProperties": false
}
